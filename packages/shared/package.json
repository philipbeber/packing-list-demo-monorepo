{
  "name": "packing-list-shared",
  "version": "0.0.0",
  "description": "> TODO: description",
  "author": "Philip Beber <philip.beber@gmail.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "watch": "npx nodemon --ignore dist/ --exec 'npx tsc && rsync -a --exclude=node_modules --delete ./ ../mobile-app/node_modules/packing-list-shared/' -e js,ts"
  },
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@reduxjs/toolkit": "^1.6.1",
    "@types/lodash": "^4.14.173",
    "lodash": "^4.17.21",
    "nodemon": "^2.0.12",
    "react-redux": "^7.2.5",
    "redux-persist": "^6.0.0",
    "short-uuid": "^4.2.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@types/redux-persist": "^4.3.1",
    "tsyringe": "^4.6.0"
  }
}
